[pytest]
addopts = -ra
markers =
    unit: fast deterministic tests that should run on every change
    integration: tests that exercise interactions between components or subprocesses
    governance: diagnostics and human-in-loop compliance checks

